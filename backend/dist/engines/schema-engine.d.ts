import type { GeneratedPage, BusinessProfile } from '../types';
type SchemaObject = Record<string, unknown>;
/**
 * Generate an array of JSON-LD schema objects for a given page.
 */
export declare function generateSchema(page: GeneratedPage, profile: BusinessProfile, locale: string): SchemaObject[];
export {};
//# sourceMappingURL=schema-engine.d.ts.map